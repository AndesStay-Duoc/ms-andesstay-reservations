# Copiar a .env y completar. El archivo .env NO se versiona.

# --- Base de datos: esquema RESERVATIONS ---
DB_URL=jdbc:oracle:thin:@//localhost:1521/FREEPDB1
DB_USER=reservations
DB_PASSWORD=cambiar_esto

# --- Servicio de catalogo, para reservar y liberar cupo ---
SVC_CATALOG_URL=http://localhost:8082

# --- Mensajeria ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=andesstay
RABBITMQ_PASSWORD=cambiar_esto

# --- Identidad ---
JWT_ISSUER=https://login.microsoftonline.com/<tenant-id>/v2.0
JWT_AUDIENCE_STAFF=api://<staff-client-id>
JWT_AUDIENCE_GUEST=api://<guest-client-id>
